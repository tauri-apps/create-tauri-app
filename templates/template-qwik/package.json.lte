{
  "name": "{% project_name_pascal_case %}",
  "description": "Qwik App with Tauri",
  "engines": {
    "node": "^18.17.0 || ^20.3.0 || >=21.0.0"
  },
  "engines-annotation": "Mostly required by sharp which needs a Node-API v9 compatible runtime",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "qwik build",
    "build.client": "vite build",
    "build.preview": "vite build --ssr src/entry.preview.js",
    "build.server": "vite build -c adapters/static/vite.config.js",
    "dev": "vite --mode ssr",
    "dev.debug": "node --inspect-brk ./node_modules/vite/bin/vite.js --mode ssr --force",
    "fmt": "prettier --write .",
    "fmt.check": "prettier --check .",
    "lint": "eslint \"src/**/*.js*\"",
    "preview": "qwik build preview && vite preview --open",
    "start": "vite --open --mode ssr",
    "qwik": "qwik",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@builder.io/qwik": "^1.13.0",
    "@builder.io/qwik-city": "^1.13.0",
    "@types/eslint": "8.56.10",
    "@types/node": "20.14.11",
    "eslint": "8.57.0",
    "eslint-plugin-qwik": "^1.13.0",
    "prettier": "3.3.3",
    "undici": "^7.9.0",
    "vite": "5.3.5",
    "vite-jsconfig-paths": "^2.0.1",
    "@tauri-apps/api": "{% if v2 %}^2{% else %}^1{% endif %}"{% if v2 %},
    "@tauri-apps/plugin-opener": "^2"{% endif %},
    "@tauri-apps/cli": "{% if v2 %}^2{% else %}^1{% endif %}"
  }
} 