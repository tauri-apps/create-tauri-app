/// <reference types="@builder.io/qwik" />
/**
 * Every TypeScript module that re-exports (or references) Qwik types should
 * call `useRecommendedPluginSettings()` to ensure consistent plugin settings.
 */ 